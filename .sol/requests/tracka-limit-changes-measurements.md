For `stu_maya_chen` and query `moles first`, the existing omitted/default call returns 26 conditional memories and 78 always-injected memories; blindly applying the declared default of 20 would reduce the conditional count from 26 to 20. I therefore changed the simulator default to `limit=None` and apply the ceiling only when a caller explicitly supplies a limit, preserving existing measurements while making explicit calls agree with the real client's `top_k` semantics.
