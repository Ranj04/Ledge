An explicitly supplied retrieval `limit` ceilings only conditional memories after per-type budgets are applied; always-injected memories remain outside the ceiling because they are policy, not retrieval. Omitting `limit` preserves the simulator's existing uncapped default behavior because the seeded corpus currently yields 26 conditional memories and an implicit limit of 20 would change existing measurements.
